<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>修改学生</title>
</head>
<body>
  <h1>修改学生</h1>
  <form method="GET" action="/doEditStudent/<%= student.sid %>">
  <!-- 当表单disabled。那么表这个get不会提交 -->
    <P>学号：<input type="number" disabled name="sid" value="<%= student.sid %>"></P>
    <P>姓名：<input type="text" name="name" value="<%= student.name %>"></P>
    <P>年龄<input type="number" name="age" value="<%= student.age %>"></P>
    <P>性别
      <input type="radio" name="sex" value="女" <% if(student.sex == "女"){%> checked <%}%>>女 
      <input type="radio" name="sex" value="男" <% if(student.sex == "男"){%> checked <%}%>>男
    </P>
    <button type="submit">确认修改学生</button>
  </form>
</body>
</html>